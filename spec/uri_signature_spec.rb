require 'spec_helper'

describe UriSignature do
  it 'has a version number' do
    expect(UriSignature::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
